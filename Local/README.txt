This folder contains computer-specific device addresses and files that
start control pannels assuming them. 
The folder needs to be stored locally on the computer, the shared files are 
for reference only and have to not be accessed instead of the local ones.